const appConfig = {
	API_PREFIX: `${import.meta.env.VITE_SERVER_API}/api`,
	AUTHENTICATED_ENTRY_PATH: "",
	AUTHENTICATED_ENTRY_PATH_ADMIN: "dashboard",
	UN_AUTHENTICATED_ENTRY_PATH: "/loginIn",
	TOUR_PATH: "",
};

export default appConfig;
